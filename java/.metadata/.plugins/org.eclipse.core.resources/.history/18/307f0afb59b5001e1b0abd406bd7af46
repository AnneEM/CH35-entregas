package ciclos;

public class ContinueLoop {

	public static void main(String[] args) {
		/*
		 * Instrucciones de salto:
		 * Continue--interrumpe la interaccion del bucle, si se procede una condicion específica y continua con la siguiente.
		 * 
		 */
		
		//Con continue, ejecutando la sentencia condicional, pero cumpliento la condicion del bucle for. 
		//Un cliclo for que cuenta deo 1 al 100 y revisa numeros diisibles entre 4 o 9
		
		for(int num =1; num <= 100; num++) {
			if(num %4 == 0) {
				System.out.println("Numero número " + num + " es divisible");
				continue; //En este caso evita que aparezcan dobles los numeros
			} System.out.println("Numero numero " + num);
		}
			
			
	
		/* for (int cliente = 1; cliente <= 20; cliente++) {
			 if(cliente == 5) {
				 System.out.println("Cliente número " + cliente + " Ganaste!!!!");
				 continue;
				 
			 } System.out.println("Eres el cliente número " + cliente); */
		
		
		
		
		
	}

}
